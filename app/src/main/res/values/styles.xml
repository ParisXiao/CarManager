<resources>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="windowActionBar">false</item>-->
        <!--<item name="windowNoTitle">true</item>-->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppThemeRed" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--<item name="windowActionBar">false</item>-->
        <!--<item name="windowNoTitle">true</item>-->
        <item name="colorPrimary">@color/colorPrimaryRed</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDarkRed</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="toolbar" parent="Base.Widget.AppCompat.Toolbar">
        <item name="android:background">@color/colorPrimary</item>
    </style>

    <style name="myProgressBarStyleLoad">
        　　
        <item name="android:indeterminateDrawable">@drawable/rotate_myprogress_load</item>
        　　
        <item name="android:minWidth">@dimen/progress_size</item>
        　　
        <item name="android:maxWidth">@dimen/progress_size</item>
        　　
        <item name="android:minHeight">@dimen/progress_size</item>
        　　
        <item name="android:maxHeight">@dimen/progress_size</item>
    </style>

    <style name="myProgressBarStyleUpdata">
        　　
        <item name="android:indeterminateDrawable">@drawable/rotate_myprogress_updata</item>
        　　
        <item name="android:minWidth">@dimen/progress_size</item>
        　　
        <item name="android:maxWidth">@dimen/progress_size</item>
        　　
        <item name="android:minHeight">@dimen/progress_size</item>
        　　
        <item name="android:maxHeight">@dimen/progress_size</item>
    </style>
    <style name="CustomerCarDialog" parent="Base.Theme.AppCompat.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    <style name="MyActivityDialogStyle" parent="Base.Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item> 设置dialog的背景，此处为系统给定的透明值
        <item name="android:windowFrame">@null</item>                Dialog的windowFrame框为无
        <item name="windowNoTitle">true</item>　　　　　　　　 是否显示标题
        <item name="android:windowIsFloating">true</item>            是否浮现在activity之上
        <item name="android:windowIsTranslucent">true</item>         是否半透明
        <item name="android:windowContentOverlay">@null</item>       是否有覆盖
        <item name="android:windowCloseOnTouchOutside">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>   设置Activity出现方式
        <item name="android:backgroundDimEnabled">true</item>        背景是否模糊显示
    </style>
    <style name="AppTheme.Daemon">
        <!--设置背景透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口框架为空-->
        <item name="android:windowFrame">@null</item>
        <!--设置没有 title-->
        <item name="android:windowNoTitle">true</item>
        <!--设置浮动-->
        <item name="android:windowIsFloating">true</item>
        <!--窗口动画为空-->
        <item name="android:windowAnimationStyle">@null</item>
        <!--不可预览-->
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowNoDisplay">false</item>
    </style>

</resources>
